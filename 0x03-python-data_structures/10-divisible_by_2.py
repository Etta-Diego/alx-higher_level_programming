#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    return [num % 2 == 0 for num in my_list]
    """# my_lists = my_list
   # length = len(my_lists)
    if not my_lists:
        return None
#    for num in range(0, length):
        if my_lists[num] % 2 == 0:
           return my_lists[num] % 2, True
        else:
           return my_lists, False """
